<!DOCTYPE html>
<html lang="en">

<head>

  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <meta name="description" content="">
  <meta name="author" content="">

  title=title

  <!-- Bootstrap core CSS -->
  link(rel='stylesheet',href='https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css',integrity='sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T',crossorigin='anonymous')    

  <!-- Custom fonts for this template -->
  link(href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css", rel="stylesheet", integrity="sha384-wvfXpqpZZVQGK6TAh5PVlGOfQNHSoD2xbE+QkPxCAFlNEevoEH3Sl0sibVcOQVnN", crossorigin="anonymous")
  link(href="https://cdnjs.cloudflare.com/ajax/libs/simple-line-icons/2.4.1/css/simple-line-icons.css", rel="stylesheet")
  link(href="https://fonts.googleapis.com/css?family=Lato:300,400,700,300italic,400italic,700italic", rel="stylesheet")

  <!-- Custom styles for this template -->
  link(href="/public/css/landing-page.min.css", rel="stylesheet")

</head>

<body>

  <!-- Navigation -->
  <nav class="navbar navbar-light bg-light static-top">
    <div class="container">
      <a class="navbar-brand" href="#">Yelp Fusion Api Demo</a>
    //-   <a class="btn btn-primary" href="#">Sign In</a>
    </div>
  </nav>

  <!-- Masthead -->
  <header class="masthead text-white text-center">
    <div class="overlay"></div>
    <div class="container">
      <div class="row">
        <div class="col-xl-9 mx-auto">
          <h1 class="mb-5">Search the business using Yelp Fusion</h1>
        </div>
        <div class="col-md-10 col-lg-8 col-xl-7 mx-auto">
          <form>
            <div class="form-row">
              <div class="col-12 col-md-5 mb-2 mb-md-0">
                <input type="input" name="location" class="form-control form-control-lg" placeholder="Enter Location">
              </div>
              <div class="col-12 col-md-5 mb-2 mb-md-0">
                <input type="input" name="term" class="form-control form-control-lg" placeholder="Enter query...">
              </div>
              <div class="col-12 col-md-2">
                <button type="submit" class="btn btn-block btn-lg btn-primary">Search</button>
              </div>
            </div>
          </form>
        </div>
      </div>
    </div>
  </header>

    if businesses
        div(class="container", style="margin: 20px 0; background: #CCC;")
            h2="Below are the top five results based on the ratings"

        <section class="showcase">
            <div class="container-fluid p-0">
            each business in businesses
                div(class="row no-gutters" style="margin: 20px")
                    div(class="col-lg-6 text-white showcase-img", style="background-image: url('"+business.image_url+"');")
                    div(class="col-lg-6 my-auto showcase-text")
                        h2
                            a(href=business.url, target="_blank")=business.name    
                        p="Rating: "+business.rating  
                        p="Price: "+business.price
                        p="Categories: "
                            each cat in business.categories
                                span(class="badge badge-secondary", style="margin: 5px 5px 5px 0;")=cat.title
                        - let address=""
                        each line in business.location.display_address
                            - address+=line
                        p="Address: "+address
                        p="Review "
                        if business.review_count > 0
                            div(id="review-carousel"+business.id, class="carousel slide", data-ride="carousel")
                                ol(class="carousel-indicators")
                                    each review, index in business.reviews
                                        li(data-target="#review-carousel"+business.id, data-slide-to=index, class=(index==0)?"active":"")
                                div(class="carousel-inner", style="padding: 5px; background-color: #CCC;")
                                    each review, index in business.reviews
                                        div(class="carousel-item "+((index==0)?"active":""))
                                            h3=review.user.name
                                            p=review.text
                                a(class="carousel-control-prev", href="#review-carousel"+business.id, role="button", data-slide="prev")
                                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                                    <span class="sr-only">Previous</span>
                                a(class="carousel-control-next", href="#review-carousel"+business.id, role="button", data-slide="next")
                                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                                    <span class="sr-only">Next</span>
                                
            </div>
        </section>

  <!-- Bootstrap core JavaScript -->
  script(src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js")
  script(src='https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js',integrity='sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM', crossorigin='anonymous')

</body>

</html>
